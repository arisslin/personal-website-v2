import { createGlobalStyle } from 'styled-components';

const GlobalColors = createGlobalStyle`
  :root {
    --color-dark: #131b20;
    --color-light: #fafafa; 
    --color-primary: #44525a;
    --color-primary-hover: #4f636e;
  }
`;

export default GlobalColors;
