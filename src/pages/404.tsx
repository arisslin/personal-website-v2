import * as React from 'react';
import Layout from '../common/components/Layout/Layout';

const NotFoundPage = () => {
  return <Layout title='404 - not found'>404 Page</Layout>;
};

export default NotFoundPage;
